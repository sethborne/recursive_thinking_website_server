export const PATHS = {
  pathMockDataRet: './_mock_data_fill/',
  pathServerDataRet: './_database_fill/',
  // pathCurrProjDataRet: '../../recursive_thinking_website_react_sandbox/recursive_thinking_website_react_cra/src/_data_returns/',
};
export const UTF_FORMAT = 'utf8';

const PREFIX = 'RecursiveThinking';
const SUFFIX = 'IdArray';

export const JSON_NAME = {
  idsUsers: `${PREFIX}Users_${SUFFIX}.json`,
  users: `${PREFIX}Users.json`,
  idsLessons: `${PREFIX}Lessons_${SUFFIX}.json`,
  lessons: `${PREFIX}Lessons.json`,
  idsInterviewQuestions: `${PREFIX}InterviewQuestions_${SUFFIX}.json`,
  interviewQuestions: `${PREFIX}InterviewQuestions.json`,
  idsInterviewQuestionAnswers: `${PREFIX}InterviewQuestionsAnswers_${SUFFIX}.json`,
  interviewQuestionAnswers: `${PREFIX}InterviewQuestionsAnswers.json`,
  idsSkills: `${PREFIX}Skills_${SUFFIX}.json`,
  skills1: `${PREFIX}Skills1.json`,
  skills2: `${PREFIX}Skills2.json`,
  idsRanks: `${PREFIX}Ranks_${SUFFIX}.json`,
  ranks: `${PREFIX}Ranks.json`,
  idsHomeScreenQuotes: `${PREFIX}HomeScreenQuotes_${SUFFIX}.json`,
  homeScreenQuotes: `${PREFIX}HomeScreenQuotes.json`,
};
